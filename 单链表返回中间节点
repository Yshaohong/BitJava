# BitJava

    //给定一个带有头结点 head 的非空单链表，返回链表的中间结点。如果有两个中间结点，则返回第二个中间结点
    //求链表的长度
    public list Len(list first){
        list temp=first;
        int len=0;
        while (temp!=null){
            len++;
            temp=temp.next;

        }
        System.out.println(len);
        //返回
        temp=first;
        for (int i = 0; i <len/2 ; i++) {
            temp=temp.next;
        }
        return temp;
    }









    //打印链表
    public void print(){
        list temp=first;
        while(temp!=null){
            System.out.print(temp.getVal());
            System.out.print(" ");
            temp=temp.next;
        }
        System.out.println();
    }




